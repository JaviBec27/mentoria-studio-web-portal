import { NewlineformatPipe } from './newlineformat-pipe';

describe('NewlineformatPipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineformatPipe();
    expect(pipe).toBeTruthy();
  });
});
